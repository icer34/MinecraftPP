#version 330 core

layout (location = 0) in vec3 pos;

uniform mat4 outlineModel;
uniform mat4 view;
uniform mat4 perspProj;

void main()
{
    gl_Position = perspProj * view * outlineModel * vec4(pos, 1.0);
}
